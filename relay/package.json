{
  "name": "auralis-relay",
  "version": "1.0.0",
  "private": true,
  "description": "Listen-together relay for Auralis: one Durable Object per room, providing the shared clock two phones cannot agree on by themselves.",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "smoke": "node scripts/smoke.mjs"
  },
  "license": "ISC",
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260918.1",
    "typescript": "^5.9.3",
    "vitest": "^5.0.1",
    "wrangler": "^4.135.0",
    "ws": "^8.18.0"
  }
}
